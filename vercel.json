{
    "version": 2,
    "buildCommand": "npm run routes:generate && npm run build",
    "outputDirectory": "dist",
    "functions": {
        "dist/server.js": {
            "runtime": "node20.x"
        }
    },
    "env": {
        "NODE_ENV": "production"
    }
}
